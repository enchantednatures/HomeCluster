---
apiVersion: v1
kind: Namespace
metadata:
  name: knative-serving
  labels:
    kustomize.toolkit.fluxcd.io/prune: disabled
    # Enable Istio injection for user workloads only (KNative services)
    istio-injection: enabled
