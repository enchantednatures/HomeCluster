---
apiVersion: sailoperator.io/v1
kind: Istio
metadata:
  name: default
spec:
  namespace: istio-system
  updateStrategy:
    type: RevisionBased
    inactiveRevisionDeletionGracePeriodSeconds: 30
  version: v1.24.3
  values:
    pilot:
      resources:
        requests:
          cpu: 100m
          memory: 2048Mi
      env:
        # Disable CNI and use init containers instead
        EXTERNAL_ISTIOD: "false"
        PILOT_ENABLE_WORKLOAD_ENTRY_AUTOREGISTRATION: "true"
        # Disable automatic DestinationRule creation for kubernetes API
        PILOT_ENABLE_KUBERNETES_GATEWAY_API: "false"

    global:
      logAsJson: true
      meshID: enchantednatures
      # Use init containers instead of CNI to avoid Cilium conflicts
      istioNamespace: istio-system
      # Disable CNI completely
      # cni:
      #   enabled: false
      tracer:
        zipkin:
          address: tempo.monitoring.svc.cluster.local:9411
    meshConfig:
      accessLogFile: /dev/stdout
      accessLogEncoding: JSON
      enableTracing: true
      defaultConfig:
        holdApplicationUntilProxyStarts: true
        tracing:
          sampling: 10
      enablePrometheusMerge: true
      extensionProviders:
        - name: otel-tracing
          opentelemetry:
            port: 4317
            service: tempo.monitoring.svc.cluster.local
        - name: authentik
          envoyExtAuthzHttp:
            service: ak-outpost-istio-forward-auth.authentik.svc.cluster.local
            port: 9000
            pathPrefix: /outpost.goauthentik.io/auth/envoy
            failOpen: false
            includeRequestHeadersInCheck:
              - authorization
              - cookie
              - x-forwarded-for
              - x-forwarded-proto
              - x-forwarded-host
            headersToUpstreamOnAllow:
              - x-authentik-username
              - x-authentik-groups
              - x-authentik-email
              - x-authentik-name
              - x-authentik-uid
            headersToDownstreamOnDeny:
              - content-type
              - set-cookie
              - location
