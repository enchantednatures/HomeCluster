---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/v1/podmonitor.json
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: kellnr-db
  namespace: kellnr
  labels:
    app.kubernetes.io/name: kellnr-db
spec:
  jobLabel: app.kubernetes.io/name
  namespaceSelector:
    matchNames:
      - kellnr
  selector:
    matchLabels:
      cnpg.io/cluster: kellnr-db
  podMetricsEndpoints:
    - port: metrics
      interval: 30s
