---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Kiali moved to platform phase since it depends on Istio CRDs
  # This kustomization is now empty as core Istio components are in bootstrap/system
  []
