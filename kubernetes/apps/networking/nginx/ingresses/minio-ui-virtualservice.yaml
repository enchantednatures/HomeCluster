---
# kubernetes/apps/networking/nginx/ingresses/minio-ui-virtualservice.yaml
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: minio-ui
  namespace: networking
  annotations:
    external-dns/is-public: "true"
    external-dns.alpha.kubernetes.io/target: external.${SECRET_DOMAIN}
spec:
  hosts:
    - "minio.${SECRET_DOMAIN}"
  gateways:
    - istio-ingress/external-gateway
  http:
    - route:
        - destination:
            host: minio-ui
            port:
              number: 9769
