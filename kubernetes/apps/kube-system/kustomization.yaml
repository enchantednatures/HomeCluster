---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Core components moved to bootstrap phases, keeping empty for now
  # Additional system services that can come later
  # - ./coredns/ks.yaml
  []
