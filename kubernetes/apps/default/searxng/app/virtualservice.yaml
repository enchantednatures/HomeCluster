---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: searxng
  namespace: default
spec:
  hosts:
    - search.enchantednatures.com
  gateways:
    - istio-ingress/external-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: searxng.default.svc.cluster.local
            port:
              number: 8080