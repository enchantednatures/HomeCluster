apiVersion: v1
kind: ConfigMap
metadata:
  name: loki-bucket
  namespace: monitoring
data:
  BUCKET_NAME: "loki"
  BUCKET_ENDPOINT: "s3://loki"
  BUCKET_HOST: "s3.{SECRET_DOMAIN}"
  REGION: us-east-1
