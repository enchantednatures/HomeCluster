---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: console
  namespace: redpanda
spec:
  hosts:
    - console.${SECRET_DOMAIN}
  gateways:
    - istio-ingress/external-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: console.redpanda.svc.cluster.local
            port:
              number: 8080
